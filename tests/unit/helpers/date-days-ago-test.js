import { dateDaysAgo } from '../../../helpers/date-days-ago';
import { module, test } from 'qunit';

module('Unit | Helper | date days ago');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dateDaysAgo(42);
  assert.ok(result);
});
